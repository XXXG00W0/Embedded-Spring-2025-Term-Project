{
    "target_overrides":{
        "*": {
            "platform.minimal-printf-enable-floating-point": true,
            "target.components_add": ["BLE"],
            "target.features_add": ["BLE"]
        }
    }
}